# Aurora Flux

Cel-shaded arcade aurora racing game built with Three.js, TypeScript, Vite, and Bun.

## Quick start

```
bun install
bun run dev
```

Then open http://127.0.0.1:5173/.

## Scripts

- `bun run dev` — start the Vite dev server
- `bun run build` — typecheck and produce a production build
- `bun run lint` — run ESLint
- `bun run check-types` — run TypeScript checks
- `bun run verify:race` — verify deterministic checkpoints, laps, ranking, and wrong-way state
- `node tools/capture-screenshots.mjs` — run the Playwright browser harness against the dev server

## Structure

- `apps/game` — the game app (Three.js + Vite)
- `packages/typescript-config` — shared `tsconfig` base
- `packages/eslint-config` — shared ESLint flat config
- `spec/` — game vision, architecture, milestones, acceptance criteria
- `tasks/current.md` — current milestone and objectives
- `AGENTS.md` — operating rules for AI agents working on this codebase

## Status

The repository contains the playable release loop through M6:

- Three-lap aurora circuit with countdown, checkpoints, finish, and restart
- Three procedural AI opponents and position tracking
- Procedural hazards (including dark matter storms), landmarks, riders, HUD,
  minimap, results, and Web Audio
- Adaptive quality scaling and browser screenshot verification

No downloaded assets are used. Meshes, shaders, UI, and sound are generated by
the codebase.

## Verification

With the dev server running, use:

```text
bun run check-types
bun run lint
bun run build
bun run verify:race
node tools/capture-screenshots.mjs
```

## License

MIT
